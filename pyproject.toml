[project]
name = "tsql"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "asyncpg>=0.30.0",
    "pytest>=8.3.5",
]
